---
import { FaEnvelope } from 'react-icons/fa6';

interface Props {
  title?: string;
  description?: string;
}

const {
  title = "Let's Work Together",
  description = "I'm always interested in new opportunities and challenging projects. Whether you need a frontend developer, fullstack solution, or technical consultation, let's connect.",
} = Astro.props;
---

<section class="bg-primary border-border rounded-lg border p-8 text-center">
  <h2 class="text-on-primary mb-4 text-3xl font-bold">{title}</h2>
  <p class="text-on-primary mx-auto mb-6 max-w-2xl">
    {description}
  </p>
  <div class="flex justify-center">
    <a href="/contact" class="bg-secondary hover:bg-secondary-hover text-on-secondary inline-flex items-center gap-2 rounded-lg px-6 py-3 font-medium transition-colors">
      <FaEnvelope className="icon-sm" />
      Get In Touch
    </a>
  </div>
</section>
