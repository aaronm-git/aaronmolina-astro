---
// This page will be accessible at /admin
---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<meta name="robots" content="noindex" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<title>Content Manager</title>
	</head>
	<body>
		<div id="nc-root"></div>
		<!-- Buffer polyfill for DecapCMS -->
		<script src="https://cdn.jsdelivr.net/npm/buffer@6.0.3/index.min.js"></script>
		<script is:inline>
			// Polyfills for DecapCMS
			window.global = window;
			
			// Process polyfill
			if (typeof window !== 'undefined' && !window.process) {
				window.process = { env: {} };
			}
			
			// Additional polyfills that might be needed
			if (typeof window !== 'undefined' && !window.Buffer) {
				// Fallback if CDN doesn't work
				window.Buffer = {
					from: function(data) {
						return new Uint8Array(data);
					},
					isBuffer: function(obj) {
						return obj instanceof Uint8Array;
					}
				};
			}
		</script>
		<script is:inline type="module" src="/src/scripts/cms.js"></script>
	</body>
</html>
